VITE_API_BASE_URL=http://localhost:4000

PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-production

# SMTP settings for monthly account statements
# If SMTP_HOST is not set, emails will be logged to the backend console only.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Kerikeri Car Storage <no-reply@example.com>"

